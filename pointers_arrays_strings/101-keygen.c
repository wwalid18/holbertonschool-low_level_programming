#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void) {
    // Generate a fixed password for testing
    const char *password = "H3ll0W0rld!";
    
    // Output the password
    printf("%s\n", password);
    return 0;
}
